[phases.setup]
nixPkgs = ['nodejs-18_x', 'npm-9_x']

[phases.install]
cmd = 'npm ci'

[phases.build]
cmd = 'echo "No build step required"'

[start]
cmd = 'node server.js'

[variables]
NODE_ENV = 'production'